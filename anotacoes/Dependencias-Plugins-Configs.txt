# Instalando "node" e "npm"
$   sudo apt-get install nodejs -y
$   sudo apt-get install npm -y

# Instalando "lodash"
$   npm i lodash -y

# Projeto com Express:
$   npm init -y
$   npm i --save express@4.16.3 -E
$   npm i --save body-parser@1.18.2 -E
$   npm i --save-dev nodemon


# - Plugin VSCode JavaScript:
#     runner (Code Runner) > Install
#     Ctrl + Alt + N (Executar o JS)

