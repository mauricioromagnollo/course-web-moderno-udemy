/**
 * Retorna o índice no qual o elemento foi encontrado, ou -1 caso não encontre!
 * @returns { elementIndex || -1 }
 */

const numeros = [2, 3, 12, 11]

console.log(numeros.lastIndexOf(12))
console.log(numeros.lastIndexOf(19))
console.log(numeros.lastIndexOf(11,2))